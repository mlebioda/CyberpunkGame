import { Component, OnInit } from '@angular/core';
import { WebSocketService } from './websocket.service';
/*
@Component({
  selector: 'app-game',
  
})
export class GameComponent implements OnInit {
  sessionId = "1234"; // Example session
  user = "Player1";
  sessionData: any;

  constructor(private wsService: WebSocketService) {}

  ngOnInit() {
    this.wsService.joinSession(this.sessionId, this.user);
    this.wsService.onUpdateSession((data) => this.sessionData = data);
  }

  startGame() {
    this.wsService.startGame(this.sessionId);
  }

  nextPhase() {
    this.wsService.nextPhase(this.sessionId);
  }
}*/
